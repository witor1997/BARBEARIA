# Estilo Livre - Barbearia


```powershell
# Inicializar repositório
git init
git add .
git commit -m "Initial commit - site Estilo Livre"
# criar repositório no GitHub e adicionar remote (substitua a URL abaixo)
git remote add origin https://github.com/SEU_USUARIO/BARBEARIA.git
git branch -M main
git push -u origin main
```

Dicas:
- Teste responsividade com DevTools (Chrome) em diferentes resoluções.
- Substitua as imagens em `img/` por fotos reais da barbearia.
- Customize cores em `css/style.css` ou usando variáveis do Bootstrap.
